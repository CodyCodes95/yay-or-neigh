import { type FormWithFields } from "~/types/prismaRelations";

type Props = {
  form: FormWithFields
};

const Form: React.FC<Props> = ({ form }) => {
  return <div>Form</div>;
};

export default Form;
